/* Starting data seeded into storage on first run.
   Deliberately EMPTY: this repository is public, so no course, student, mark or
   date may live here. A fresh visitor gets a clean slate and the built-in
   syllabi from syllabi.js; their own work stays in their own browser.
   Keep SEED_STAMP unchanged — bumping it makes applyBundle() delete saved
   syllabus and layout keys from every existing browser. */
export const SEED_STATE = {};
export const SEED_STAMP = "2026-07-22T00:30:11.314Z";
